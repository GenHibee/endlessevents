[package]
name = "EndlessEvents"
version = "1.0.0"
authors = ["Endless Events Team"]

[addresses]
endless_events = "_"  # Set during deployment

[dependencies]
EndlessFramework = { git = "https://github.com/endless-labs/endless-core.git", subdir = "endless-move/framework/endless-framework", rev = "main" }

